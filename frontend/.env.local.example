# Copy this file to .env.local and adjust if needed.
# .env.local is gitignored.

# Base URL of the FastAPI backend. Safe to expose to the browser — it's a
# URL, not a secret. No API keys or credentials belong on the frontend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000